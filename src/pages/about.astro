---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "Sobre mi";

const identify = {
    firsName: "Sarah",
    country: "Canada",
    occupation: "Developer",
    hobbies: ["running", "sky diving", "singing"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Tailwind"];
const happy = true;
const finished = false;
const goal = 3;
const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
    <h2>... ¡Y mi nuevo sitio Astro!</h2>
    <p>
        Estoy trabajando en el tutotial introductorio de Astro. Esta es la
        segunda página de mi sitio ¡y la primera que he construido yo mismo!
    </p>
    <p>
        Este sitio se irá actualizando a medida que vaya terminando mas partes
        del tutorial, ¡así que no dejes de visitarlo para ver como va mi viaje!
    </p>
    <p>He Aquí algunos datos sobre mi</p>
    <ul>
        <li>Me llamo {identify.firsName}.</li>
        <li>
            Vivo en {identify.country} y trabajo como {identify.occupation}.
        </li>
        {
            identify.hobbies.length >= 2 && (
                <li>
                    Mis hobbies son: {identify.hobbies[0]} y{" "}
                    {identify.hobbies[1]}
                </li>
            )
        }
    </ul>
    <p>Mis habilidades son:</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
    {happy && <p>¡Estoy feliz de aprender Astro!</p>}

    {finished && <p>¡He terminado este tutorial!</p>}

    {
        goal === 3 ? (
            <p>Mi objetivo es terminar en 3 días.</p>
        ) : (
            <p>Mi objetivo no son 3 días.</p>
        )
    }

    <style define:vars={{ skillColor, fontWeight, textCase }}>
        h1 {
            color: purple;
            font-size: 4rem;
        }

        .skill {
            color: var(--skillColor);
            font-weight: var(--fontWeight);
            text-transform: var(--textCase);
        }
    </style>

</BaseLayout>
